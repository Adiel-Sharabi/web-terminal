# NOTE: the Gradle wrapper (gradlew, gradlew.bat, gradle-wrapper.jar) IS tracked
# here so a fresh clone can build Android without the standalone repo.
/.gradle
/captures/
/local.properties
GeneratedPluginRegistrant.java
.cxx/

# Remember to never publicly share your keystore.
# See https://flutter.dev/to/reference-keystore
key.properties
**/*.keystore
**/*.jks
