node_modules/
*.log
config.json
config.json.bak
config.test.json
sessions.json
sessions.json.bak
history.json
scrollback/
clipboard-images/
test-results/
*.crt
*.key
*.pem
ssl/
*.p12
*.pfx
*.jks
*.keystore
.env
.env.*
*.stackdump
cluster-tokens.json
api-tokens.json
claude-session-names.json
.session-secret
.hook-token
*.png
# The AiTerminal client's icons/launcher assets are real source — exempt the
# whole subtree from the repo-wide *.png ignore (that rule is for server shots).
!ai-terminal/**/*.png
logs/
monitor-status.json
*.restart-bak
test-emu-*.js
test-exclusive-*.js
# local scratch / experiment files (never published)
check-server.js
evloop-test.js
local-burst-test.js
pty-test.js
pty-test2.js
raw-pipe-test.js
server-evloop.js
tests/diagnostic-report.json
playwright.diag.config.js
cdp_*.py
sessions.test.json
.tmp-*
.wt_inputrc
.claude/

# Per-session push notification levels (ntfy)
notify-prefs.json

# Per-session favorites/pin ranks (#60 — the server-side SSOT for the pinned group)
favorites.json

# FCM device registry (companion app push tokens — semi-sensitive)
push-devices.json

# local test rig instance (scripts/rig/rig.js creates this outside the repo)
/wt-rig/

# Mirrored Claude status-line metrics (#72 — keeps idle sessions ctx across a restart)
claude-metrics.json

# Metrics mirror written by the test run (isolated from the real one)
claude-metrics.test.json
